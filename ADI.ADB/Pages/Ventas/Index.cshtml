@page
@model ADI.ADB.Pages.Ventas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ventas[0].TotalFacturado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ventas[0].TotalDescuento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ventas[0].Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ventas[0].Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ventas[0].Empleado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ventas) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TotalFacturado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalDescuento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empleado.Apellido)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
