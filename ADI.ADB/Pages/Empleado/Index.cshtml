@page
@model ADI.ADB.Pages.Empleado.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EMPLEADO[0].NOMBRE)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EMPLEADO[0].APELLIDO)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EMPLEADO[0].EMAIL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EMPLEADO[0].TELEFONO)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EMPLEADO[0].DIRECCION)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EMPLEADO[0].ID_ROLENavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EMPLEADO) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NOMBRE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.APELLIDO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EMAIL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TELEFONO)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DIRECCION)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_ROLENavigation.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
