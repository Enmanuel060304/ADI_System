@page
@model ADI.ADB.Pages.VENTAS.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VENTum[0].FECHA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VENTum[0].ID_CLIENTENavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VENTum[0].ID_EMPLEADONavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VENTum) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FECHA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_CLIENTENavigation.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_EMPLEADONavigation.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
